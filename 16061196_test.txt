const int tp_f = 10, tp_0 = 20;
const char ch_s = 'a';
const char ch_f = 'w';
int pt, tp, array[1];
char op, ch;
int fib(int x){
  if(x<=1)
    return (x);
  if(x>1){
    return (fib(x-1) + fib(x-2));
  }
}
void func(int x, int y){
  const int u = 24;
  const char c = 'z';
  if(y >= x){
    tp = - u * 3 + c;
    ch = '8';
    array[1] = 1;
    array[1] = 0;
  }
  if(y < x) {
    tp = fib(x);
    ch = '_';
    array[0] = 0;
    array[1] = 1;
  }
}
 void main(){
  int n;
  n = 0;
  scanf(pt);
  scanf(pt, op);
  if(pt == op)  printf("pt equals to op");
  switch (op) {
    case 'f':{
      tp = fib(pt) + tp_f;
      ch = 'a';
      array[0] = 1;
      array[1] = 1;
    }
    case 'k':
      func(pt, tp_f);
    default:{
      tp = 0;
      ch = 'a';
      array[0] = 0;
      array[1] = 0;
    }
  }
  while(n < tp_f){
    printf(" fib ", n + 1);
    printf(" ", fib(n + 1));
    n = n + 3;
  }
  printf("tp = ", tp);
  printf(" ,");
  printf("ch = ", ch);
  printf(" ");
  printf(array[5] + 'a' +array['b'-'a']);
}




