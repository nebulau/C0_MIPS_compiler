const char const_char1 = 'a';
int x;
char op;
char globalarr[5];
int fib(int x){
    if(x<=2)
        return (1);
    if(x >2)
        return (fib(x-1)+fib(x-2));
}
char retchar(){
    return ('-');
}

void testif(){
    int x, y;
    scanf(x);
    scanf(y);
    printf("x is");
    printf(x);
    printf("y is");
    printf(y);
    if(x>y)
        printf("x>y the greater is ",x);
    if(x>=y)
        printf("x>=y ",x);
    if(x<y)
        printf("x<y the greater is ",y);
    if(x<=y)
        printf("x<=y ",y);
    if(x==y)
        printf("x==y ");
    if(x!=y)
        printf("x!=y ");
    {}
    ;
    {}{}{};
    {{{}}}
    ;;;{;{;}};
}
void testwhile(){
    int arr[4], _A2RR2[4];
    int i;
    i = 0;
    while(i<=3){
        arr[i] = i + i *i -i;
        printf(arr[i]);
        i = i+1;
    }
    while(i>4);
    x=5;
    arr[0]=23;
    _A2RR2[+23-arr[arr[0]-+23]] = -1+-2+-3+-4++5++5+6+9+fib(5)+'*'+retchar()+1*-2+2+2/1-2 + const_char1 +x;
    printf(_A2RR2[0]);
}
void testassign(){
    int i;
    globalarr[0]='+';
    globalarr[1]='/';
    globalarr[2]='a';
    globalarr[3]='5';
    globalarr[4]='A';
    
    i=0;
    while(i<5){
        printf(globalarr[i]);
        i=i+1;
    }
    x=15;
    if(x)   printf(x);
    x=0;
    if(x)   printf(x);
}
void testswitch(){
    switch(x){
        case 1:
            printf(1);
        case 2:
            printf(2);
        default:
            printf(0);
    }
    switch(op){
        case 'a':
            printf('a');
        case 'z':
            printf('z');
        default:
            printf('*');
    }
}
void callfunc(){
    int i;
    printf("input a choice:");
    scanf(i);
    switch(i){
        case 1:
            testif();
        case 2:
            testwhile();
        case 3:
            testassign();
        case 4:{
            scanf(x);
            scanf(op);
            testswitch();
        }     
    }
}
void main(){
    int i;
    scanf(i);
    printf("i = ", i);
    callfunc();
    return;
}
